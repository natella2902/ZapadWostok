$(document).ready(function(){
  $('.owlDots').owlCarousel({
    items: 1,
    center:true,
    nav:false,
    loop:true,
    dots: true,
    dotsClass: 'container-dots',
    dotClass: 'circle-btn'

  });
});