$(document).ready(function(){
  $('.owlArrows').owlCarousel({
    items: 1,
    center:true,
    nav:true,
    loop:true,
    dots: false,
    dotsClass: 'container-dots',
    dotClass: 'circle-btn'

  });
});